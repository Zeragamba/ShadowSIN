version: "3.9"
services:
  db:
    image: postgres
    ports:
      - "5432:5432"
    volumes:
      - ./db/data:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: shadowsin
      POSTGRES_PASSWORD: "${DB_PASSWORD}"
